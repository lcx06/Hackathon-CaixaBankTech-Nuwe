import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'caixabank_hackathon',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
